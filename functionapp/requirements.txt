# Azure Functions (Flex Consumption) — Python runtime dependencies.
#
# Mirrors the runtime deps in ../pyproject.toml so the serverless host runs the
# SAME orchestrator code as the FastAPI host. Keep these in sync. The optional
# RAG extra (azure-search-documents) is included so cloud retrieval works.
azure-functions>=1.21,<2.0

azure-ai-projects~=2.2.0
azure-identity>=1.19,<2.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0
structlog>=24.4,<25.0
azure-search-documents>=11.5,<12.0
